name: BTC 1H Signal Telegram V3

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Ourbit connection
        run: |
          echo "Testing Ourbit..."

          curl -I \
            -A "Mozilla/5.0" \
            --max-time 20 \
            "https://futures.ourbit.com"

          echo "Ourbit connection test finished."
